import React from 'react';
// import PropTypes from 'prop-types';

const Header = () => {
    return (
        <header className="uk-margin" uk-margin="true">
            <h1 className="uk-text-center">
                All events you might be looking for
            </h1>
        </header>
    );
};

Header.propTypes = {

};

export default Header;